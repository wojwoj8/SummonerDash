const ProfileGames = (props) =>{

    const {games} = props;
    console.log(games)
    return (
        <div>
            {/* {games.map((game) =>(
                <div>

                 </div>
            ))}
             */}
            
        </div>
    )
}

export default ProfileGames;